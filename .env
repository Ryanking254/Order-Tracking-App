# Kept on React Navigation (not expo-router file routing).
# SDK 56+ forbids @react-navigation/* imports when expo-router is present
# (it ships transitively inside @expo/cli), so opt out of that check.
# See https://docs.expo.dev/router/migrate/sdk-55-to-56/
EXPO_ROUTER_DISABLE_RN_NAVIGATION_CHECK=1
